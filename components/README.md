# Components

Shared React presentation is organized by product surface:

- `ui/`: shadcn/Radix primitives.
- `chat/`: agent conversation UI.
- `editor/`: file tree and editor UI.
- `preview/`: runtime preview and diagnostics.
- `source-control/`: browser Git and change review UI.
- `workbench/`: composition for the main IDE shell.
